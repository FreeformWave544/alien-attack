[gd_resource type="Shader" format=3 uid="uid://b2de23j1sgayj"]

[resource]
code = "shader_type canvas_item;

void fragment() {
	vec2 uv = UV - vec2(0.5); // Center the circle
	float dist = length(uv);
	if (dist > 0.5) {
		discard; // Make it a perfect circle
	}
}
"
